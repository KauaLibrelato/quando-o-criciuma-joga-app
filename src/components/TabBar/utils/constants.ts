import {TabButtonProps} from './types';

export const tabButtonProps: TabButtonProps = {
  accessibilityRole: 'button',
  accessibilityState: {selected: true},
  activeOpacity: 0.7,
  onPress: () => {},
  onLongPress: () => {},
};
